- Create modules by writing Python code in `.py` files.
- Import modules using `import module`, `from module import name`, or aliasing.
- Modules enable code reuse and organisation across Python projects.

```bash
pip install numpy
```


```python
from numpy import array
arr = array([1, 2, 3])
print(arr)
```

```python
import numpy
# OR
import numpy as np

#component 
```
